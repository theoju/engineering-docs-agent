{"jira_issues": [{"key": "CCE-75", "summary": "orchestrator: _stage_docs_run_changes pathspec breaks on hosts that already gitignore .docs-agent-plugin", "description": "Symptom\nADIS docs-agent-nightly silently exited 1 in _stage_docs_run_changes. Surfaced by CCE-73 observability fix on first post-merge dispatch (run 26770782104) as:\ndocs-agent: open_or_append_pr git_add_failed: The following paths are ignored by one of your .gitignore files:\n.docs-agent-plugin\nhint: Use -f if you really want to add them.Root cause: CCE-70 plugin/host coordination conflict\nCCE-70 made two changes:\nPlugin side (scripts/orchestrator_runner.py:_stage_docs_run_changes): added negative pathspec :!.docs-agent-plugin :!.docs-agent-plugin/** to the git add . invocation, preventing the vendored plugin checkout from being staged as a gitlink (mode 160000)}, added .docs-agent-plugin/ to ADIS/.gitignore to undo the existing gitlink contamination.

When both are present, git add . -- ':!.docs-agent-plugin' is rejected by git with exit 1 because the negative pathspec causes git to treat the path as "explicitly mentioned" — and then .gitignore rejects it anyway. Root cause is the pathspec/.gitignore interaction, not either change alone.

ADIS host side (PR #396 cleanup): added .docs-agent-plugin/ to ADIS's .gitignore to undo the existing gitlink contamination.

When both are present, git add . -- ':!.docs-agent-plugin' is rejected by git with exit 1 because the negative pathspec causes git to treat the path as "explicitly mentioned" — and then .g…[truncated]", "status": "Done", "labels": ["orchestrator", "p2", "regression"]}]}