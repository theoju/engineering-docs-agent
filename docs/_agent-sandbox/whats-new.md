## 2026-05-21T17:23:41.994283+00:00
- PR #12: Fixed a root-cause bug in the Claude CLI plugin manifest: the `author` field in `.claude-plugin/plugin.json` was a plain string, but the CLI's Zod validator requires an object with a `name` property. This caused the plugin to silently fail-load on every dispatch since commit 628a7262 (2026-05-19), meaning all prior CCE-12, CCE-14, and CCE-15 source-collector measurements had actually captured default Claude Code behavior rather than the subagent. The fix is accompanied by a new JSON Schema draft-07 file (`templates/plugin_manifest.schema.json`) and five pytest regression tests guarding against author-as-string and other manifest shape errors. A 5-run Mode B re-baseline was executed, confirming 5/5 successful plugin loads and 5/5 tool invocations; 25 forensic artifacts were committed under `docs/superpowers/measurements/`. Supersession notices were prepended to the CCE-12, CCE-14, and CCE-15 baseline docs to preserve audit-trail accuracy.
- PR #11: Three layered fixes address source-collector dispatch failures identified in CCE-14. First, `dispatch_subagent` now passes `--setting-sources project,local` to exclude user-level Claude settings (where a prose-injecting SessionStart hook resides) while preserving OAuth keychain access. Second, `agents/schemas/source_collector.schema.json` gains `additionalProperties: false` at the top level and inside `prs.items`, so phantom keys like prose-as-key entries now fail validation visibly rather than passing silently. Third, a new `_rescue_json_object` helper in `scripts/orchestrator_runner.py` performs brace-balanced JSON extraction when strict `json.loads` fails, recording `prose_contamination_rescued` in a threaded `out_reasons` list that surfaces in `partial_reasons`. A 5-run Mode B re-measurement baseline doc and full run artifacts are committed under `docs/superpowers/measurements/`. Result is PARTIAL PASS: contamination and phantom-silent goals met; Category-A structural bypass carry-forward to CCE-16.
- PR #10: Hardens the source-collector agent prompt by restructuring its Procedure into a mandatory ordered checklist with explicit 'MAY NOT proceed until step N completed AND evidence is visible in tool-call history' gates, adding a §5 Forbidden-outputs entry for the Category-A failure mode (returning empty prs:[] without calling gh pr list), and numbering all five Forbidden-outputs entries §1–§5 for unambiguous cross-reference from the Procedure. Fixes a latent bug in _extract_final_assistant_text in scripts/orchestrator_runner.py so that when the final assistant turn is pure tool_use, the last text-bearing turn is returned instead of an empty string. Adds two tests covering the new and the previously-uncovered all-tool-only path. Commits full 5-run Mode B measurement artifacts (meta, prompt, stdout, stderr, stream.jsonl) under docs/superpowers/measurements/ and a companion plan and spec doc for the CCE-14 work.
- PR #9: Adds a debug-capture mode to `dispatch_subagent` in `scripts/orchestrator_runner.py`, gated by the `DOCS_AGENT_DEBUG_DIR` environment variable. When the gate is on, dispatch switches to Claude's stream-JSON output format and records the source-collector agent's complete tool-call sequence (raw NDJSON + a structured tool-use summary) to disk; when the gate is off, dispatch behavior is byte-for-byte unchanged. Two pure helper functions (`_extract_final_assistant_text` and `_summarize_tool_use`) back the new path, each covered by new TDD tests (10 new tests, 174 total passing). The PR also ships a 5-run Mode B baseline measurement doc that categorizes each run as A (zero tool calls), B (tools called then discarded), C (legitimately empty), or D (errored). Four of five runs fell into Category A — the source-collector emitted canonical empty JSON in a single ~3–6 s turn without ever invoking a tool.
- PR #8: The source-collector agent now reliably emits canonical `{"prs":[...],"jira_issues":[...]}` JSON on Mode B runs. Four confirmed root causes were closed: (1) the global stop-verify hook was contaminating child-process stdout with a verification preamble that broke `json.loads` — fixed by passing `CLAUDE_STOP_VERIFY=0` in the `dispatch_subagent` subprocess env; (2) the agent was reflexively emitting status-report objects (`{"status":"idle",...}`) instead of data — fixed by adding a `## Forbidden outputs` subsection to `agents/source-collector.md` with concrete bad-shape examples; (3) the field name `issues` (and variants) was being used instead of the required `jira_issues` key — the forbidden-outputs section now names `jira_issues` as the only valid key; (4) a meta-refusal regression where the agent reframed the embedded payload as untrusted and declined to act — a fourth forbidden-outputs entry explicitly prohibits refusal, deferral, and clarification-request responses. A redundant legacy `## Output contract` block was also removed from the agent prompt. Validation: 5/5 consecutive Mode B runs against the CCE-11 self-host harness emit canonical-shape JSON, up from 0/5 pre-fix.
- PR #7: Wires the engineering-docs-agent plugin to run against its own repository as a dogfood and reference host. Adds `.engineering-docs-agent/config.yml` and `state.example.json` to configure the agent for this repo, creates `docs/_agent-sandbox/` as the agent-writable target directory (restricted via a narrow `agent_editable_paths` glob that keeps writes out of `docs/superpowers/` and other tracked dirs), updates the README with a "Self-hosting (dogfood)" bootstrap section, and pins the seed `last_successful_run.head_sha` to the v0.1.0 tag so the source-collector has a real diff window over CCE-1…CCE-9 history. Publish-verification is explicitly deferred: `--no-pr` keeps the initial bootstrap run dry-run only until a `deploy.yml` workflow is added.
- PR #6: PR #6 ships two changes against the source-collector reliability investigation (CCE-9). First, `scripts/orchestrator_runner.py` gains opt-in diagnostic capture: when `DOCS_AGENT_DEBUG_DIR` is set, `dispatch_subagent` switches to `--output-format stream-json --verbose` and writes per-subagent artifacts (prompt, extracted stdout, raw NDJSON stream, stderr, and a meta.json with a tool-use summary) to that directory. Behavior is byte-identical to v0.1.3 when the variable is unset. Two new unit tests cover the capture path (suite: 163/163). Second, `agents/source-collector.md` gains an explicit Step 0: when `last_sha` is empty, the agent must emit `{"prs": [], "jira_issues": []}` and stop — this is now the only valid skip path for Steps 1–5. The PR also adds Phase 1 and H4 measurement artifacts (raw stdout captures, state.json snapshots from three Mode B runs, and a validation summary) under `docs/superpowers/measurements/`, plus an investigation plan at `docs/superpowers/plans/2026-05-20-cce9-source-collector-reliability.md`. The source-collector reliability fix is partial: the agent now cites empty `last_sha` and early-exits per Step 0, but still emits a non-canonical `{"status": "idle", ...}` shape. Two new root causes (stop-verify hook stdout contamination and the agent's status-report reflex) are tracked in CCE-10.
- PR #5: A bug in `scripts/orchestrator_runner.py` caused `state.current_run.partial_reasons` (and `partial: true`) to carry forward from prior orchestrator invocations into subsequent runs. The state-initialization block was reordered so every new run unconditionally begins with a fresh `current_run` — `partial: false` and `partial_reasons: []` — before checking whether the prior run was stale. The stale-clear diagnostic (`stale_current_run_cleared`) is preserved by writing into the newly-initialised `current_run` via `add_partial`. The old `carried_partial` / `carried_reasons` reads were deleted. Three subprocess-driven integration tests (`tests/orchestrator/test_state_carry_forward.py`) lock the no-carry-forward contract, and `CHANGELOG.md` was updated with a v0.1.3 entry.
- PR #4: Introduces a `dispatch_validated()` wrapper at the LLM-to-typed-code boundary in `scripts/orchestrator_runner.py` and `scripts/verify_runner.py`, wired into 8 of the 9 subagent call sites. Off-contract LLM responses now surface as specific `schema_invalid: <name>: <field-detail>` reasons in `state.current_run.partial_reasons` instead of being silently swallowed by `dict.get()` fallbacks (the v0.1.1 Mode-B silent-no-op bug). All 7 agent `.md` files gain an authoritative `## Output schema (canonical)` section embedding the JSON Schema and a "Return ONLY a JSON object" instruction. Two new test suites are added: a parameterized drift-prevention lint (7 cases asserting `.md` schema blocks stay JSON-equivalent to `agents/schemas/*.schema.json`) and an end-to-end soft-fail integration test reproducing the observed wrong shape and asserting the specific reason surfaces. Test count grows from 146 to 158 with no new runtime dependencies.
- PR #3: Completes production wiring of `dispatch_subagent` across three dimensions: (1) JSON payloads are now wrapped in `<inputs>…</inputs>` framing with pinned execution instructions (`Execute the Job`, `Return ONLY`, `no prose`, `no markdown fences`, `no clarifying questions`) so the LLM executes the job rather than analyzing the raw JSON; (2) `subprocess.run()` receives an explicit `cwd` argument threaded through all nine call sites in `orchestrator_runner.py` and `verify_runner.py` so each agent loads the correct `CLAUDE.md`; (3) `--plugin-dir` (auto-detected via `Path(__file__).resolve().parent.parent`) and `--allowedTools Bash Read Write Edit WebFetch` are added to the CLI argv so agent plugins resolve without a user install and can execute their declared tools non-interactively. Ten new unit tests cover all three fixes plus failure-mode preservation; the full suite runs 146 green.
- PR #2: Fixed a silent total-failure in `dispatch_subagent` (`scripts/orchestrator_runner.py`) where every subagent call was returning `None` because the orchestrator used the legacy `claude agent <name> --input <json>` CLI form, which does not exist in Claude Code v2.x. The corrected invocation is `claude --setting-sources project,local -p <framed-prompt> --agent <name> --plugin-dir ... --allowedTools ...`. Six new unit tests in `tests/orchestrator/test_dispatch_subagent.py` lock in the updated argv shape, the execution-framing contract, and all four failure-mode branches (missing binary, non-zero exit, empty stdout, unparseable JSON). No behavior was changed beyond the argv fix; error handling is identical to before.
- PR #1: Introduces the engineering-docs-agent Claude Code plugin in its entirety. The v0.1.0 baseline ships seven coordinated subagents — source-collector, pr-summarizer, page-author, content-validator, gap-detector, publish-verifier, and notifier — that convert merged PRs, commits, and Jira issues into a single nightly docs-update PR with voice-matched authoring, tiered linting, gap detection, and post-merge publish verification. The v0.1.1 hardening release folds in all 40 reviewer findings: typed contracts backed by JSON schemas, contract wiring (Jira forwarding, voice samples, path filtering, archive indexes, pr_id alignment), error-handling hardening across GhClient and the orchestrator runner, regression test backfill, and final polish (exit-code normalization, schema tightening, multi-summary batching, CHANGELOG). The combined release ships with 160+ passing tests across a full red-green-verify TDD cycle.
### Gaps flagged
- theoju/engineering-docs-agent#11: No dismissed flag, no allowlist paths, and size_filter thresholds are unset so the size gate cannot fire. LLM judgment: this PR introduces three behavioral changes to the orchestration core — a new --setting-sources argv flag in dispatch_subagent, a brace-balanced JSON rescue helper (_rescue_json_object) with a new out_reasons threading parameter, and schema tightening (additionalProperties: false) that changes validation outcomes. Each of these alters user-visible or system-visible behavior in a non-trivial way. A senior engineer would expect a written design rationale (especially for the --bare vs --setting-sources tradeoff and the rescue algorithm). Notably the PR already includes docs/superpowers/specs/2026-05-21-cce15-source-collector-root-cause-sweep-design.md and a plan document, confirming the author judged a spec necessary as well.
- theoju/engineering-docs-agent#8: No allowlist paths configured and no size thresholds set, so LLM judgment applies. The PR modifies a subagent's output contract (agents/source-collector.md), changes orchestrator subprocess execution behavior (env overlay in scripts/orchestrator_runner.py), and closes four root causes affecting the canonical output shape of the source-collector. Changes to subagent contracts and orchestrator runtime behavior are the kind of systemic, multi-cause fixes a senior engineer would expect to see accompanied by a written design spec and implementation plan. The PR itself includes both (docs/superpowers/specs/ and docs/superpowers/plans/), confirming the team treated this as spec-worthy.
- theoju/engineering-docs-agent#6: Not in dismissed_flags. No allowlist paths configured. Size filter has no thresholds defined, so proceeding to LLM judgment. The PR modifies a subagent contract (agents/source-collector.md, adding a new Procedure step that changes observable agent behavior) and introduces a new orchestrator feature (DOCS_AGENT_DEBUG_DIR diagnostic capture in scripts/orchestrator_runner.py). Both changes affect system-level behavior visible to operators and downstream agents. A senior engineer would expect a written spec or plan for a behavioral contract change to a subagent and a new env-gated diagnostic subsystem. Notably, the PR itself includes docs/superpowers/plans/2026-05-20-cce9-source-collector-reliability.md, confirming the team's own bar for this change warranted a plan.
- theoju/engineering-docs-agent#4: This PR introduces a new validated-dispatch boundary (`dispatch_validated`) wired into 8 call sites across two runners, changes the observable failure behavior from silent no-ops to surfaced `schema_invalid` reasons in pipeline state, and updates the canonical contracts of all 7 agent prompts. It is a cross-cutting architectural change with user-visible behavioral impact (error surfacing) and a new public function in the orchestrator API surface. A senior engineer would expect a written spec and implementation plan. (The PR itself ships both under `docs/superpowers/specs/` and `docs/superpowers/plans/`, confirming the expectation is warranted.)
- theoju/engineering-docs-agent#1: Not in dismissed_flags. No allowlist_paths configured. size_filter has no thresholds (empty object), so size check is skipped. LLM judgment: this PR introduces an entirely new Claude Code plugin system — seven coordinated subagents, typed contracts, JSON-schema validation, orchestration scripts, a marketplace manifest, and 160+ tests. It creates a new public subsystem with new user-visible behavior (nightly docs-update pipeline), new public APIs (agents/*.md contracts, agents/schemas/*.json, skills/), and significant architectural decisions. A senior engineer would expect a written spec and plan. Notably, the PR itself includes docs/superpowers/specs/2026-05-20-v0.1.1-hardening-design.md and docs/superpowers/plans/2026-05-20-v0.1.1-hardening.md, confirming that spec-level documentation is warranted and was produced.

