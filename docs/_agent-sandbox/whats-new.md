## 2026-05-21T22:18:06.436761+00:00
- PR #16: PR #16 adds a two-layer defense against out-of-window PRs bleeding into the source-collector's output. First, the orchestrator (`scripts/orchestrator_runner.py`) gains a new `_clip_prs_to_window()` helper that runs `git rev-list last_sha..head_sha` and hard-drops any PR whose `merge_sha` is not in the resulting commit set, surfacing structured reasons (`out_of_window_dropped`, `merge_sha_missing`, `out_of_window_filter_skipped`) before downstream stages ever see the data. Second, the source-collector prompt (`agents/source-collector.md`) is tightened: Step 1 now resolves both SHA bounds and applies both `merged:>=` and `merged:<=` filters; a new Step 1.5 mandates a per-PR `git rev-list` membership check; Step 6's self-checklist gains a rev-list verification bullet; and Forbidden outputs §7 codifies the exact contract violation shape observed in CCE-16 runs. Five new tests cover four `_clip_prs_to_window` edge cases plus one end-to-end integration scenario.
- PR #15: Fixes a silent Jira-enrichment failure in the source-collector subagent where curl requests to Jira's REST API were made without authentication credentials. When `JIRA_EMAIL` / `JIRA_API_TOKEN` were absent from the invoking shell, Jira returned deliberate 401-as-404 responses and the agent silently dropped `jira_issues` from its output with no error signal. The fix is three-layered: (1) three regression tests pinning `dispatch_subagent`'s full-env-passthrough so future allowlist changes must consciously decide whether to drop Jira credentials; (2) a full rewrite of source-collector Step 5 documenting the env-var contract, `curl -f` auth-failure detection, the auth-missing probe pattern, and the canonical `partial: true` + `error: "jira_auth_missing"` graceful-degrade shape — plus a §6 split into §6a (fabrication) and §6b (silent-drop) to close the wording gap that technically permitted silent drops; (3) schema-level tests pinning the `jira_auth_missing` partial shape as valid, and a new README 'Jira enrichment' section documenting the operational env-var requirement and fallback behavior.
- PR #14: The pr-summarizer agent received a three-layer defense-in-depth fix for `page_hint` contract violations. The agent prompt's §6 was rewritten with explicit per-action rules and a new §Forbidden outputs section (§1–§5) documenting five categories of bad shape — lens-prefix-doubled paths, source-tree paths, out-of-sandbox creates, empty doc_targets on non-trivial changes, and prose-wrapped output. The JSON schema (`agents/schemas/pr_summarizer.schema.json`) was updated to enforce portable structural rules universally: relative path, `.md` extension, no source-file extensions, `lens` constrained to `{core, superpowers}`, and `additionalProperties: false`. Ten regression fixtures captured from the 2026-05-21 failing run were added under `tests/fixtures/cce17/`, alongside three new test modules covering schema enforcement, replay behavior, and end-to-end soft-fail handling through the `dispatch_validated` → schema-check → `partial_reasons` chain.
- PR #12: Fixed a silent plugin load failure in `.claude-plugin/plugin.json` where the `author` field was set as a plain string instead of the object `{ "name": "..." }` required by the Claude CLI's Zod schema validator. Because this bug was present since the plugin's first commit (2026-05-19), every prior CCE-12/14/15 measurement captured default Claude Code responding to the orchestrator prompt — not the source-collector subagent. A JSON-Schema draft-07 regression guard (`templates/plugin_manifest.schema.json`) and 5 new pytest tests were added to prevent recurrence. A fresh 5-run Mode B baseline was then executed against the now-working source-collector agent: 5/5 plugin loads confirmed, 5/5 tool invocations, 2/5 correct empty outputs, 3/5 out-of-window PR returns (new failure mode deferred to CCE-17). Supersession notices were prepended to the CCE-12, CCE-14, and CCE-15 baseline docs, and the CCE-16 real-baseline doc plus a plan doc were committed under `docs/superpowers/`.
- PR #11: Three defense-in-depth fixes were applied to the source-collector dispatch pipeline. First, `dispatch_subagent` argv now passes `--setting-sources project,local` to exclude user-level Claude settings (which host a SessionStart hook that injects explanatory prose), while preserving OAuth keychain access. Second, `source_collector.schema.json` was tightened with `additionalProperties: false` at the top level and inside `prs.items`, so phantom fields like prose-as-keys now raise `schema_invalid` instead of silently passing validation. Third, a new `_rescue_json_object` helper was added to `scripts/orchestrator_runner.py` performing brace-balanced JSON extraction with string-state tracking; when strict `json.loads` fails, dispatch falls through to it and records `prose_contamination_rescued` in a new `out_reasons` list threaded through `dispatch_validated`. A 5-run Mode B re-measurement was executed and committed, yielding PARTIAL PASS: contamination 0/5 and phantom-silent 0/5 goals met, but the structural Category-A bypass rate was unchanged — carried forward to CCE-16.
- PR #10: PR #10 hardens the `source-collector` agent prompt by restructuring its Procedure into a mandatory ordered checklist where each step requires visible tool-call evidence before the next may begin, and adds §5 to Forbidden Outputs explicitly naming the Category-A failure mode (empty `prs: []` returned without ever invoking `gh pr list`). All five Forbidden-output sections are numbered §1–§5 for direct cross-reference from the Procedure. In `scripts/orchestrator_runner.py`, `_extract_final_assistant_text` is fixed to prefer the last text-bearing assistant turn over the unconditional last turn, closing a footgun where a final tool-only assistant message silently dropped otherwise-correct output. Two new pytest tests cover both edge-case paths. A 5-run Mode B re-measurement was executed and committed under `docs/superpowers/measurements/` alongside updated spec and plan docs.
- PR #9: Adds a debug-gated stream-JSON dispatch path in `dispatch_subagent` (`scripts/orchestrator_runner.py`) that captures the source-collector subagent's complete tool-call sequence during Mode B runs when the `DOCS_AGENT_DEBUG_DIR` environment variable is set. When the gate is off, dispatch behavior is byte-for-byte unchanged. Two pure helpers — `_extract_final_assistant_text` and `_summarize_tool_use` — are introduced with full TDD coverage (10 new tests; 174 total pass). The caller contract (`dispatch_subagent → dict | None`) is preserved: in stream-JSON mode the dict is parsed from the final assistant message's text content. Ships a 5-run Mode B baseline measurement categorizing each run as A (zero tool calls) / B (called and discarded) / C (legitimately empty) / D (errored); 4 of 5 runs are Category A, confirming the source-collector completes a ~3–6 s turn without ever invoking a tool.
- PR #8: Fixes four confirmed root causes that prevented source-collector from emitting canonical {"prs":[...],"jira_issues":[...]} JSON on Mode B runs. The orchestrator's dispatch_subagent now passes CLAUDE_STOP_VERIFY=0 in the subprocess environment so the global stop-verify hook cannot inject a verification preamble into child Claude stdout (which broke json.loads). The source-collector agent prompt gains a Forbidden Outputs subsection with four concrete bad-shape examples, explicitly prohibiting status-report reflexes, the legacy 'issues' key name, and the meta-refusal 'untrusted payload' reframe. The redundant legacy Output contract block was removed. Post-fix validation shows 5/5 consecutive Mode B runs emitting canonical-shape JSON, up from 0/5 pre-fix.
- PR #7: Wires the engineering-docs-agent plugin to run against its own repository as a dogfood host. Adds `.engineering-docs-agent/config.yml` and `state.example.json` so the orchestrator has a real configuration target, restricts agent writes to `docs/_agent-sandbox/**` via a narrow `agent_editable_paths` glob, and seeds `last_successful_run.head_sha` to the v0.1.0 tag so the source-collector has a meaningful diff window over CCE-1 through CCE-9 history. The bootstrap runs with `--no-pr` (dry-run only) until a `deploy.yml` publish-verification workflow is added. README gains a new 'Self-hosting (dogfood)' section documenting the bootstrap pattern.
- PR #6: PR CCE-9 ships two main changes: (1) a `DOCS_AGENT_DEBUG_DIR` environment variable added to the orchestrator runner that, when set, captures per-subagent prompt, stdout, stderr, and metadata to disk — enabling systematic diagnostics without altering normal execution (unset is byte-identical to v0.1.3); (2) an explicit empty-`last_sha` early-exit (step 0) added to the source-collector agent spec, instructing the agent to return a canonical empty response and halt when `last_sha` is absent. The PR also lands Phase 1 and H4 measurement artifacts documenting the investigation. The source-collector fix is partial: the step-0 instruction shifts agent behavior but the agent still emits a non-canonical `{"status": "idle"}` shape; two new root causes (stop-verify hook contaminating stdout, and the agent's status-report reflex overriding canonical-shape instructions) are forwarded to CCE-10.
- PR #5: Fixed a bug where `state.current_run.partial_reasons` carried forward across orchestrator invocations. The state-init block in `scripts/orchestrator_runner.py` was reordered so each new run now constructs a fresh `current_run` with `partial: false` and `partial_reasons: []` before the stale-check runs. The stale-clear diagnostic (`stale_current_run_cleared`) is preserved by writing into the fresh state via `add_partial`. The old `carried_partial` / `carried_reasons` reads are deleted. Three new integration tests in `tests/orchestrator/test_state_carry_forward.py` lock the no-carry-forward contract.
- PR #4: Introduces a `dispatch_validated()` wrapper at the LLM-to-typed-code boundary in `orchestrator_runner.py` and `verify_runner.py`, composing the existing `dispatch_subagent` with `contracts.validate_and_parse`. Off-contract LLM responses now surface as specific `schema_invalid: <name>: <field-detail>` reasons in `state.current_run.partial_reasons` instead of being silently swallowed by `dict.get()` fallbacks (the v0.1.1 Mode-B silent-no-op bug). Eight call sites are wired: six in `orchestrator_runner.py` and two in `verify_runner.py`; a ninth fire-and-forget notifier call is intentionally left on the raw dispatch. All seven agent `.md` files gain an embedded `## Output schema (canonical)` block making the JSON Schema authoritative in the system prompt itself. A parameterized drift-prevention lint (`tests/agents/test_schema_md_sync.py`, 7 cases) asserts `.md` schema blocks stay JSON-equivalent to their corresponding `agents/schemas/*.schema.json` files. An end-to-end soft-fail integration test (`tests/orchestrator/test_schema_invalid_soft_fail.py`) reproduces the Mode-B observed wrong shape against the full pipeline. Test count grows from 146 to 158; no new runtime dependencies; soft-fail contract from v0.1.1 is preserved.
- PR #3: Completed production wiring of `dispatch_subagent` by fixing three compounding issues at the subprocess dispatch boundary. (A) Prompt framing: payloads are now wrapped in `<inputs>...</inputs>` markers with pinned execution instructions (`Execute the Job`, `Return ONLY`, `no prose`, `no markdown fences`, `no clarifying questions`) so the LLM executes rather than analyzes the JSON. (B) CWD propagation: `subprocess.run()` now accepts a `cwd: Path | None = None` parameter and forwards it; all nine call sites in `orchestrator_runner.py` and `verify_runner.py` thread `repo_root` through so each agent loads the correct `CLAUDE.md`. (C) Plugin discovery and tool permissions: argv now includes `--plugin-dir` (auto-resolved via `Path(__file__).resolve().parent.parent`) and `--allowedTools` with the full union of declared agent tools (`Bash Read Write Edit WebFetch`), enabling non-interactive dispatch without requiring a user-installed plugin.
- PR #2: Fixed a silent failure in `dispatch_subagent` where every subagent call returned `None` because the orchestrator was invoking `claude agent <name> --input <json>` — a CLI form that does not exist in Claude Code v2.x. The argument shape is now `claude -p <payload> --agent <name>`. The fix is a one-line argv change; the four documented failure-mode branches (FileNotFoundError, non-zero exit, empty stdout, JSON parse error) are fully preserved. Six new unit tests lock the corrected argv shape and all failure paths, bringing the suite from 136 to 142 passing tests.
- PR #1: Introduces the complete engineering-docs-agent Claude Code plugin across two releases shipped together. v0.1.0 delivers seven coordinated subagents — source-collector, pr-summarizer, page-author, content-validator, gap-detector, publish-verifier, and notifier — that convert merged PRs, commits, and Jira issues into a nightly docs-update PR with voice-matched authoring, tiered linting, gap detection, and publish verification. v0.1.1 hardens the baseline by adding typed contracts and JSON-schema validation, Jira forwarding, path filtering, archive index support, pr_id alignment, comprehensive error handling across GhClient and the orchestrator, regression test backfill (160+ passing tests), exit-code normalization, schema tightening, multi-summary batching, and a CHANGELOG — closing all 40 reviewer findings across five structured phases.
### Gaps flagged
- theoju/engineering-docs-agent#16: This PR introduces substantive new orchestrator behavior: a `_clip_prs_to_window` filtering helper that drops PRs from downstream stages based on `git rev-list` membership, plus coordinated changes to the source-collector agent prompt adding new Steps 1.5 and 6 self-checklist items. These constitute a change in system-level data flow (what PRs reach downstream stages) and a new subsystem contract. A senior engineer would expect a written spec or design plan for a change that alters which source data flows through the pipeline and codifies a new agent–orchestrator filtering contract. The PR body is detailed but serves as a post-hoc writeup; the canonical spec for agent contracts belongs in a tracked design document.
- theoju/engineering-docs-agent#14: Not in dismissed_flags. No allowlist_paths configured, so allowlist tier is skipped. size_filter has no thresholds defined, so size tier cannot exclude this PR. Applying LLM judgment: this PR introduces a new JSON schema contract (agents/schemas/pr_summarizer.schema.json) governing the output shape of the pr-summarizer subagent, rewrites the agent prompt's §6 with explicit per-action rules and a new 'Forbidden outputs' section, and adds a multi-layer defense-in-depth across schema, prompt, and orchestrator filter. These are inter-agent contract changes — new public schema, changed agent-visible behavioral rules — exactly the category a senior engineer expects a written spec or plan to accompany. The PR body itself references a parent design document (docs/superpowers/specs/2026-05-21-post-first-run-defects-design.md), confirming the change is spec-backed. Needs spec: true.
- theoju/engineering-docs-agent#11: LLM judgment: This PR introduces three substantive behavioral changes — (1) a schema contract tightening (additionalProperties: false in source_collector.schema.json, affecting what inputs are accepted or rejected), (2) a new prose-tolerant JSON rescue subsystem (_rescue_json_object in orchestrator_runner.py), and (3) a dispatch-flag change (--setting-sources project,local) with security-relevant implications for OAuth and plugin contamination. Each of these individually crosses the threshold a senior engineer would expect a written spec or plan to accompany: new subsystem behavior, changed public schema contract, and a security-sensitive dispatch configuration change. The PR itself confirms this — it includes docs/superpowers/specs/2026-05-21-cce15-source-collector-root-cause-sweep-design.md and docs/superpowers/plans/2026-05-21-cce15-source-collector-root-cause-sweep.md, which are the expected artifacts.
- theoju/engineering-docs-agent#8: No allowlist paths configured and size_filter thresholds are absent, so LLM judgment applies. The PR modifies an agent contract (agents/source-collector.md) by adding forbidden-output rules and removing an existing output-contract block, and changes subagent dispatch behavior in scripts/orchestrator_runner.py via an env overlay that affects all child Claude sessions. These are behavioral changes to a subagent's canonical output shape and its invocation environment — exactly the class of change a senior engineer would expect to see documented with a spec and plan. (The PR already includes docs/superpowers/specs/2026-05-20-cce10-source-collector-canonical-shape-design.md and docs/superpowers/plans/2026-05-20-cce10-source-collector-canonical-shape.md, confirming the expectation is met, but the gap-detector flags that such documentation is warranted.)
- theoju/engineering-docs-agent#6: Not in dismissed_flags. No allowlist paths configured. size_filter has no thresholds defined, so size-filter tier is skipped. LLM judgment: this PR modifies an agent contract file (agents/source-collector.md, adding a new Procedure step that alters runtime behavior), introduces a new diagnostic feature (DOCS_AGENT_DEBUG_DIR capture in scripts/orchestrator_runner.py), and adds new tests. Changes to agent contracts and new observable infrastructure are exactly the class of change a senior engineer would expect a written spec or plan to accompany. Notably, a plan document (docs/superpowers/plans/2026-05-20-cce9-source-collector-reliability.md) is already included in the PR, which is consistent with that expectation.
- theoju/engineering-docs-agent#4: The PR introduces a new dispatch_validated wrapper at the LLM→typed-code boundary, wires it into 8 call sites across orchestrator_runner.py and verify_runner.py, changes how off-contract LLM responses surface (specific schema_invalid reasons instead of silent no-ops), and modifies the system prompts of all 7 agent .md files. At 24 files and +2458/-17 lines this is a substantial architectural change affecting a critical pipeline boundary and fixing a silent-failure bug. A senior engineer would expect a written spec and implementation plan for a change of this scope — and the PR itself confirms that expectation by including both docs/superpowers/specs/2026-05-20-cce4-schema-enforcement-design.md and docs/superpowers/plans/2026-05-20-cce4-schema-enforcement.md.
- theoju/engineering-docs-agent#1: This PR introduces an entirely new system (the engineering-docs-agent plugin) with seven coordinated subagents, typed contracts, JSON schemas, a full test suite, and a v0.1.1 hardening release — all in a single integration. It defines new public agent APIs (agents/*.md, agents/schemas/*.json), a new user-visible workflow (nightly docs-update PR, voice-matched authoring, tiered linting, gap detection), and significant architectural decisions. This is precisely the kind of change a senior engineer expects to be preceded by a written spec or plan. The PR itself includes spec and plan documents (docs/superpowers/specs/ and docs/superpowers/plans/), confirming the expectation. New subsystem, new public API surface, new user-visible behavior — all three criteria apply.

