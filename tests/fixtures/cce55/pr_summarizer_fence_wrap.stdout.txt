```json
{"pr_number":68,"what_changed":"Added two job-level `env:` entries to `.github/workflows/docs-agent-nightly.yml` that expose the `JIRA_API_TOKEN` and `JIRA_EMAIL` repository secrets into the docs-agent-nightly runner environment. Because `source-collector` inherits the parent process environment via `dispatch_subagent`, no further plumbing was required beyond this job-env declaration. The fix eliminates the recurring `source_collector_error: jira_auth_missing` partial flag that has appeared on every docs-agent PR since the workflow went live.","why":"Three successive docs-agent PRs (#54, #64, #67) opened with `partial_reasons: [jira_auth_missing]`, meaning Jira-enriched PR summaries were never produced. The partial-mode indicator is intended to surface transient failures, not serve as the permanent steady state. Both secrets were already registered in the repo at 15:32 UTC; only the workflow job-env wiring was missing. CCE-53 tracked this gap; CCE-45 established the constraint that `secrets.*` references are valid at job-env scope but not at steps-env scope.","breaking":false,"doc_targets":[{"lens":"core","action":"create","page_hint":"operations/2026-05-29-jira-auth-nightly-runner.md"}],"notes":"The page_hint targets operations/ because this is a CI/CD workflow configuration fix with direct operational impact (nightly run reliability, Jira enrichment). No existing operations page was identified as an exact match, so a dated entry is created. Post-merge validation step (manual workflow trigger) is listed in the PR's test plan but not yet confirmed complete at merge time.","error":null,"partial":false}
```